# dialogs — эталонные диалоги

Появляются на шаге 2 плана 22: текстовые файлы «команда → ожидаемый ответ» с директивой
`#advance <мс>` виртуального времени. Один прогонщик исполняет их против хост-сборки
прошивки и против эмулятора `contr_emu` (А13); расхождение видно в CI.
