# Конфигурация тестов Python репозитория (генератор, инструменты, интерфейс).
# Прошивка собирается CMake; здесь только pytest.
[tool.pytest.ini_options]
testpaths = ["tests/py"]
pythonpath = ["tools", "ui", "emu"]
addopts = "-q"
